[pytest]
markers =
    unit: fast isolated tests that do not cross process, storage, API, or app-service boundaries
    integration: tests that cross module/service/storage/API boundaries
    import_isolation: tests that verify import side effects or import-time dependency boundaries
    slow: tests that are materially slower than normal unit tests
